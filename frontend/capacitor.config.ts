import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.nakshatra.healthcare',
  appName: 'NakshatraHealthcare',
  webDir: 'out'
};

export default config;
